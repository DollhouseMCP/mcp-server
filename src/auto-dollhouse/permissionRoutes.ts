/**
 * auto-dollhouse#5: Permission evaluation HTTP routes.
 * Re-exports from upstream implementation to avoid duplication.
 */
export { registerPermissionRoutes } from '../web/routes/permissionRoutes.js';
