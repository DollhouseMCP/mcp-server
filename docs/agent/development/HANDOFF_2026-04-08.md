# Handoff - April 8, 2026

## Current State
- Branch: `release/2.0.11-rc.1`
- Last commit: `2a0bea1b` chore: bump version to 2.0.11-rc.1
- Open PRs: none (all merged)
- Uncommitted changes: README.md swap from npm publish (restore with `git checkout README.md`)
- Published: `@dollhousemcp/mcp-server@2.0.11-rc.1` on npm (`@next` and `@rc` tags)

## What Was Done
- Fixed Auth tab in standalone --web mode and all lazy-init tab reload bugs
- Added release channel selector (Stable/RC/Beta) to Setup tab
- Unified console port config (config file -> env var -> CLI flag)
- Fixed license form visibility when commercial license active
- Published v2.0.11-rc.1 to npm

## What Needs to Happen Next
1. Test RC on multiple machines — `npx @dollhousemcp/mcp-server@next --web` should show working Auth tab, channel selector, and Configure buttons
2. If bugs found: fix on `release/2.0.11-rc.1` branch, bump to `2.0.11-rc.2`, publish with `npm publish --tag next --no-provenance`
3. When stable: create PR from `release/2.0.11-rc.1` to `main`, merge, create GitHub release, publish to `@latest`

## Active Context
- Release branch `release/2.0.11-rc.1` is NOT merged to main — intentional
- `@latest` on npm is still v2.0.10
- `develop` branch has all changes merged and is ahead of main
- CI publish workflow doesn't auto-tag prereleases — manual publish required for RCs

## Gotchas
- `npm publish --provenance` fails locally — use `--no-provenance` for manual publishes
- npm OTP required for publish and dist-tag operations
- The channel selector uses `rc` as the value but the release-lifecycle docs say `@next` is the npm tag for RCs — minor inconsistency
- Docker security tests fail locally (Docker not running) — pre-existing, not a regression
