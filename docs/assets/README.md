# Documentation Assets

Static files referenced by the documentation and README build scripts.

Current contents:

- `dollhouse-logo.png` – Logo displayed in README variants and marketing docs.

Add additional branding assets here as needed; keep the folder lightweight and note usage locations when adding files.
